# Placeholder Assets

**⚠️ Put your PNGs in `public/placeholders/`, NOT here.**

This directory (`src/assets/placeholders/`) is NOT served by Vite at runtime.
The app loads images from `/placeholders/...` which maps to `public/placeholders/`.
